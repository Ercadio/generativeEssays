From Jun Cui Subject How to hold the control to a window object I'm using BC 's ObjectWindows version and trying to get some data processed in a window However when the calling program invokes the window object it gives up the control to the window object and keeps executing the next I would like the calling program after invoking the window object to wait until the window object is Can I do that My program may look like class MyWindow public TWindow void MyCallingProg Could the calling program be a C function MyWindow MyWinObj MyWinObj new MyWindow GetApplication MakeWindow MyWinObj MyWinObj Show SW SHOWNORMAL next statement I want the program to wait here until MyWinObj is closed so that I can get some data back from I specified the window style to be WS POPUPWINDOW didn't Is there any other way to execute the window object so that the calling program won't give up the control Any help would be Jun To talk to the Lord with PS through MS Windows