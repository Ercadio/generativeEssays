From Gerard O'Driscoll Subject Re Creating bit windows on bit How writes A problem occurs when I try to create a window with a visual that is different from the visual of the parent which uses the default visual which is You've got to set border pixel in your window The default is CopyFromParent which gives the Do this unsigned long valuemask if border width is non zero you'd better alloc a colour from cmap rather than use any old pixel Also use valuemask it makes the code more cmap pixel valuemask CWColormap CWBorderPixel win XCreateWindow dpy DefaultRootWindow dpy width height border see comment below depth InputOutput class visual valuemask attr A note on border width your code looked like this win XCreateWindow dpy DefaultRootWindow dpy width height CopyFromParent border width depth InputOutput class visual CWColormap attr border width set to CopyFromParent works but doesn't make border width should be an unsigned You get away with it because CopyFromParent is define'ed to be zero in If it happened to be defined as you'd get a very interesting looking window Gerard O'Driscoll Du Pont Pixel Systems