From Casper Dik Subject Re Problems with xdm under Solaris Danny Backx writes Yes that is a bug in the port of You sometimes get this when you start from I have reported this to Casper but don't know whether it made it into his set of patches Basically in mit clients xterm there is a test at line which checks errno after attempting to open dev Add the errno EINVAL case to it and you should be It's actually a bug in the Solaris We could add a workaround to open should never return EINVAL but it does I didn't have time to update the patch lately but non of the existing problems seem to warrant a new set of Since is a Bourne shell script anyway you might as well add a line etc TIMEZONE as it is done in etc XDM coulda also keep the XDM environment It should have inherited it from the Casper