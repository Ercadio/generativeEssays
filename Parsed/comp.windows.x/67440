From Joe Kelsey Subject Re XV has escaped In George Lindholm writes In article George Lindholm writes In article John Bradley writes And here is the first patch The following fixes some problems with the Imakefile files and makes it possible to compile xv under Solaris without having to use the ucb stuff Oops I didn't get the signal processing right thats what I get from doing the work on an ascii terminal Here is the fixed patch The patches for need to use ifdef '' rather than the overly complex if defined sun defined SYSV '' in order to generically compile XV on Also rand and srand don't work very well use and to get better Otherwise xv compiles just fine on my Esix System V box with and gcc I just uncommented the gcc definition in the Makefile added L usr lib I usr include to the COPTS line and modified LIBS to include lsocket Really John if you just use the Imakefile things like this don't even have to get signal macros ifdef define HOLD SIG sighold SIGALRM define RELEASE SIG sigrelse SIGALRM define PAUSE SIG sigpause SIGALRM else define HOLD SIG sigblock sigmask SIGALRM define RELEASE SIG sigblock define PAUSE SIG sigpause endif default for most folks define random rand define srandom srand endif ifdef define random define srandom endif