From morris hirsch Subject none We are adding a Motif wrapper to a family of data display These programs are each written as a 'main loop' with these steps use semop to check a semaphore and if true access new data otherwise block new data comes quite often use msgop to check for messages and if true read them otherwise continue messages are user commands and not often update the display according to latest data and We typically have several of these running in windows plus a data producer serving some device and setting the Everyone blocking makes sure the device server gets to Xt and Xm also have a 'main loop' model that we must fit O'Reilly Vol IV Ch discusses adding a file watcher and also how to add workprocs that are run during idle We can 'open up' our existing main loop and call it as a The problem is our blocking until new data is no longer Neither is letting the program free run because others are The unix select call lets you block until any of several i o are We want that for the X main loop except not file i Any suggestions or examples most welcome We run SGI Personal Iris and Indigo Morris Hirsch aka or